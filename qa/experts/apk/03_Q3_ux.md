# Q3 — UX S01–S05 (APK / `gaucon`)

> Persona: Expert Q3 · App: **Hôm nay chơi gì?** · Module `gaucon/`  
> Charter: `qa/experts/apk/00_CHARTER.md` · So sánh ngắn vs web pilot `web/src/app.mjs`  
> Nguồn: `feature/onboarding|today|activity|reminder` · `MainActivity` / `GauConNav` · `PRODUCT_DESIGN` S01–S05 · `BUILD_READY` Wave 3

---

```
STATUS: FAIL
SCOPE: Luồng UX S01–S05 — Welcome, hồ sơ, (S03 stub), Today, chi tiết HĐ, disclaimer; dead nav; parity web ngắn.
DONE: Đọc charter + 4 feature modules + MainActivity NavHost + web app.mjs concepts; đối chiếu PRODUCT_DESIGN / BUILD_READY; ghi EDITS_REQUIRED P0/P1.
FILES: qa/experts/apk/03_Q3_ux.md
DEVIATIONS: Wave 3 cố ý bỏ S03 màn hình + bottom tab ngoài Hôm nay (BUILD_READY) — vẫn flag vì charter yêu cầu dead nav / disclaimer; không chạy thiết bị thật (static review).
BLOCKERS: Không (review mã nguồn đủ cho verdict UX).
ERRORS: Không crash cứng chứng minh được trên open Activity từ static review.
NEXT_FOR_PARENT: Q5 hợp nhất; Applier ưu tiên P0 empty-Today (seed gate) trước Closed testing.
```

---

## Map luồng mong đợi vs Android vs web

| Mã | PRODUCT_DESIGN | Android `gaucon` (Wave 3) | Web pilot |
|---|---|---|---|
| **S01** Welcome | 3 slide + Bắt đầu; không xin notif | Có pager 3 slide + CTA; copy disclaimer slide 3 | 1 hero + Bắt đầu (không pager) |
| **S02** Hồ sơ | Tên + ngày sinh bắt buộc | Có; validate enable nút; không message lỗi trống | Form + localStorage |
| **S03** Nhắc | Màn riêng + quyền | **Không màn** — silent schedule 19:30 trong `saveChild` | Không (IA note “có trên Android”) |
| **S04** Today | 2–3 thẻ + Đổi gợi ý + disclaimer | Có list + Đổi gợi ý + dòng nháp; banner notif chỉ “Để sau” | Có picks + banner disclaimer dismiss + nav đầy đủ |
| **S05** Chi tiết | Bước / safety / dễ–khó / CTA | Text-first: goal, materials, steps, safety, Hoàn thành | + phrases / easier / harder / sticky CTA |
| **S06** Feedback | Sau Hoàn thành | **Thiếu** — `complete` → popBackStack, `feedback=null` | Có 😊😐😕 + lưu |

Nav: Android bottom bar hiện 5 tab nhưng **chỉ Hôm nay enabled** (stub Wave 3). Web: Today / Hoạt động / Tài liệu / Phát triển / Thêm đều mở được.

---

## Checklist cờ charter

| Cờ | Verdict | Ghi chú |
|---|---|---|
| Blank screens | **WARN** | Boot: `Text("Đang mở…")` không `fillMaxSize`/scaffold — dễ trông trống khi edge-to-edge. S05 null → “Đang tải / Không tìm thấy” (không blank trắng). |
| Dead nav | **FAIL (P1)** | 4/5 tab `enabled=false`, `onClick={}` — PH thấy IA rồi không vào được. Wave 3 documented nhưng charter bắt flag. |
| Missing disclaimer | **FAIL (P1)** | Welcome slide 3 có “không chẩn đoán”. Today/S05 chỉ dòng `draft_unreviewed` — **thiếu** “Không chẩn đoán y tế / Mỗi bé một nhịp” và **không** banner dismiss như web. |
| Broken profile save | **WARN** | Happy path có upsert + `setOnboardingDone` + navigate. **Không** loading/error UI; exception trước `onDone()` → kẹt S02. Reminder enqueue gắn cứng vào save (rủi ro phụ). |
| Empty Today | **FAIL (P0)** | Seed `upsertIfNewer()` chạy **async** `runCatching` trong `Application` — Today `load()` không chờ seed → cold start / onboarding nhanh → `activities.isEmpty()`. Seed fail bị nuốt → empty bền. |
| Crash on activity open | **PASS (static)** | `getById` null → empty state, không NPE rõ. `domains`/`materials`/`steps` non-null lists. Không chứng minh crash; S05 `return` non-local trong `Column` là smell Compose (không chắc crash). |

---

## Top UX FAIL

**P0 — Empty Today / seed race:** `GauConApp` nạp seed nền; `TodayViewModel.load` / `GetTodayPicksUseCase` đọc Room ngay → list trống + copy “chưa có gợi ý phù hợp tuổi” (sai nguyên nhân). Core value S04 gãy ngay lần mở đầu.

---

## So sánh ngắn web

- Web: disclaimer banner toàn cục; complete/feedback S06; tab thư viện cứu empty.
- Android: disclaimer yếu hơn; không S06; không lối thoát Thư viện khi Today trống → empty **nặng hơn** web.

---

## ## EDITS_REQUIRED

### P0

| ID | Vấn đề | Sửa đề xuất |
|---|---|---|
| **UX-P0-01** | Empty Today do seed chưa sẵn / fail im lặng | Gate: Today (và/hoặc picker) **await** seed upsert thành công trước khi pick; lỗi seed → copy rõ + nút Thử lại (không nuốt `runCatching` không UI). Tránh persist `DailyPick` rỗng khi DB activity=0. |

### P1

| ID | Vấn đề | Sửa đề xuất |
|---|---|---|
| **UX-P1-01** | Disclaimer thiếu trên S04/S05 vs web + charter | Banner hoặc dòng cố định: nháp + **không chẩn đoán y tế** + mỗi bé một nhịp; optional dismiss (parity web). |
| **UX-P1-02** | Dead bottom nav 4 tab | Wave 3: ẩn tab chưa có **hoặc** stub “Sắp có” 1 màn — đừng `enabled=false` im lặng. |
| **UX-P1-03** | Profile save không feedback lỗi | Loading trên CTA; `try/catch` → snackbar; vẫn `setOnboardingDone` nếu child đã lưu dù reminder fail (tách schedule khỏi block navigate). |
| **UX-P1-04** | S05 thiếu easier / harder / parentPhrases (model đã có) | Hiện khi non-null — parity web + PRODUCT_DESIGN text-first. |
| **UX-P1-05** | Hoàn thành bỏ S06 | Tối thiểu: 3 feedback hoặc xác nhận; đừng chỉ `feedback=null` + pop (backlog nếu Wave 3 cắt cứng). |
| **UX-P1-06** | Boot blank-ish | Splash/`fillMaxSize` + progress, không một `Text` giữa màn edge-to-edge. |
| **UX-P1-07** | Banner notif S04 chỉ “Để sau” | Thêm CTA mở settings quyền (wireframe PRODUCT_DESIGN) hoặc ẩn banner đến khi có S03/S11. |

### Ngoài scope Wave 3 (ghi nhận, không P0)

- S03 màn hình riêng — deferred; silent 19:30 OK theo BUILD_READY nếu UX-P0-01 + disclaimer P1 xong.
- Tab Thư viện / Nhật ký / Phát triển — sau Wave 3.

---

## Verdict theo màn (S01–S05)

| Màn | Verdict | Ghi chú ngắn |
|---|---|---|
| S01 Welcome | **WARN** | Luồng OK; thiếu page indicator; disclaimer chỉ slide cuối. |
| S02 Profile | **WARN** | Save happy path OK; thiếu error/loading. |
| S03 Reminder UX | **N/A / stub** | Facade + enqueue trong onboarding — không màn. |
| S04 Today | **FAIL** | Empty race P0; disclaimer thiếu; dead tabs. |
| S05 Activity | **WARN** | Mở được (static); thiếu phrases/easier/harder; complete không S06. |

---

```
STATUS: FAIL
TOP_UX_FAIL: UX-P0-01 Empty Today — seed async race / silent fail → S04 trống
```
